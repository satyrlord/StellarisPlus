# VF's Buildings compatibility stub -- gestalt empires only
triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = { is_gestalt = yes }
    }
    modifier = {
        job_patrol_drone_add = $AMOUNT$
    }
}